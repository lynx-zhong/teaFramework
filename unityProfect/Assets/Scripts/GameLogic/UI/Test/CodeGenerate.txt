using UnityEngine;
using UnityEngine.UI;

public class CodeGenerate : MonoBehaviour
{
    // field auto generate start
    public GameObject Code2233563GenerateGameObject;
    public GameObject hahGameObject;
    public Image hahImage;
    public Button asdButton;
    public Button dsafffButton;
    // field auto generate end


    private void Start()
    {


        // bind function start
        asdButton.onClick.AddListener(OnasdButtonButtonClick);
        dsafffButton.onClick.AddListener(OndsafffButtonButtonClick);
        // bind function end

        Code2233563GenerateGameObject.aaad = 10
    }

    // fucntion auto generate start
    private void OnasdButtonButtonClick()
    {

    }

    private void OndsafffButtonButtonClick()
    {

    }

    // fucntion auto generate end

    private void OnDestroy()
    {
        // bind function start
        asdButton.onClick.RemoveListener(OnasdButtonButtonClick);
        dsafffButton.onClick.RemoveListener(OndsafffButtonButtonClick);
        // bind function end
    }
}
